# Bygge beslut #137 — Undersidan "Pengarna" (`/projects/:id/pengarna` i subnaven): kumulativ förbrukningskurva mot ramlinj

Datum: 2026-09-07 02:52 · Branch: cto/undersidan-pengarna-projects-id-pengarna-137 · Overlamning: #137

## Mal
Undersidan "Pengarna" (`/projects/:id/pengarna` i subnaven): kumulativ förbrukningskurva mot ramlinjen i ren server-SVG, S7.4:s två cachade ramdatum i klartext, och kostnadsrader ur `receipts.contract_part_id`/`oplanerad` (0068) — allt läst, inget räknat i vyn.

## Kravspec (claude-fable-5) — sjalvbarande, med KALLA och ARKITEKTUR
```
Underlaget är verifierat mot koden innan kraven skrevs: S7.4:s cache skriver nyckeln `prognos` med `ram_timmar`/`ram_kronor` som `{datum}` ELLER `{villkor}` (villkoren 'inget bekräftat tak'/'ingen taxa'/'ingen bokad framtid', `uppdragSvep.ts:346–355,567–578`), S6.2:s `troskellarm` ligger under egen nyckel (`uppdragSvep.ts:605`), kvittots `contract_part_id`/`oplanerad` finns sedan 0068 och renderas redan med chip i kvittolistan (`routes.ts:8006`), `UPPDRAGSSIDOR` står i `routes.ts:3531` utan `pengarna`, `.pengarad` saknas i `html.ts` och ren inline-SVG-mönstret finns i `html.ts:214–240`. 1E Del 4 listar ingen läsåtgärd för Pengarna — vyn läser en ny tjänstefunktion direkt, som Leveranserna (S10.3). Här är kravspecen:

---

**MAL** | Ny undersida "Pengarna" (`/app/c/:companyId/projects/:projectId/pengarna`) som svarar på 1D:s fråga "hur ligger vi mot ram — i timmar, kronor och enskilda kostnader?": kumulativ förbrukningskurva mot vågrät ramlinje i ren server-SVG, S7.4:s två cachade ramdatum i klartext, tröskellarmet ur cachen och kostnadsrader med `oplanerad`-markering — allt läst, inget räknat i vyn (S10.4, FR-3/FR-5/FR-33, procentförbudet NFR-11).
**KALLA** | Överlämning #137 (story S10.4 ur 1F, våg 6) + Davids ja 2026-09-07 till CTO-underlaget; PRD FR-5/FR-33/NFR-11; 1D Del 4.4 + klasstabellen (`.pengarad`); beroenden gröna i koden: `harledPrognosramar` + cachenycklarna `prognos`/`troskellarm` (uppdragSvep.ts:435,567,605), 0068:s `receipts.contract_part_id`/`oplanerad` (uppdragKostnad.ts:165).
**ARKITEKTUR** | Vyn är bara transport (tjänstelagret inuti `withTenantTransaction`, ingen SQL/affärslogik i http-lagret); en läsväg per fråga: förbrukning ur husets enda takberäkning `forbrukningForAvtal`, taxa ur `gallandeTaxa`, belopp ur `timeEntryAmountOre` (ören-heltal, aldrig float); svepvärden HÄMTAS ur cachen och härleds aldrig om, med färskhet per källa (mönster `uppdragLage.ts` `svepcache`/`troskelutfall`/FR-35); JS-fri HTML med kanons tokens och ren inline-SVG (mönster `barChart` html.ts:214); vitest+supertest mot riktig Postgres.
**KRAV-1** | Ny läsfunktion i tjänstelagret (`server/src/services/uppdragPengar.ts`) som per avtal i uppdraget returnerar: (a) kumulativ förbrukningsserie per `work_date` i minuter och ören med exakt husets predikat (`FORBRUKANDE_STATUSAR`, `billable_minutes`, `gallandeTaxa`, `timeEntryAmountOre`) så att seriens slutsumma är lika med rotdelens `billable_minutes`/`amount_ore` ur `forbrukningForAvtal`; (b) rotdelens tak (`tak_minuter`/`tak_ore`/`tak_status`); (c) cachens `prognos` och `troskellarm` med varje rads egen `kalla`/`last_nar` — `null` när svepet aldrig kört eller värdet inte bär väntad form (mönster `troskelutfall`); (d) kostnadsrader ur `receipts` vars `contract_part_id` hör till avtalets delar (alla versioner, mönster `koposterForUppdraget`) med datum, leverantör, `total_ore`, status, avtalsdelens kod och `oplanerad`, i datumordning. Okänt/främmande `project_id` eller projekt utan avtal ⇒ 404 (mönster `kravUppdrag`).
**KRAV-2** | Ny ren SVG-kurvhelper i `html.ts` (pure funktion: ingen klocka, ingen databas) som ur serien, taket och ETT cachat `Ramutfall` ritar vågrät ramlinje, heldragen kumulativ kurva över registrerad tid och streckad förlängning fram till ramdatumet — ENDAST när utfallet är `{datum}`; är utfallet `{villkor}` eller prognosen oläst ritas ingen kurva, ingen ramlinje och inget datum — villkorstexten ur cachen (resp. "svepet har inte kört") skrivs i klartext i stället, aldrig ett härlett datum.
**KRAV-3** | Vyn ritar en kurva per ram vars cachade utfall bär datum (kronor i ören, timmar i minuter) och skriver de två ramdatumen i klartext exakt ur cachens `ram_kronor`/`ram_timmar` — vyn räknar aldrig egna datum, egen takt eller egen prognos; tröskellarmet visas ur cachen med svepets färskhet (`farskhetstid`-mönstret), aldrig omräknat.
**KRAV-4** | Kostnadstabell med `.pengarad`-rader: datum, leverantör, avtalsdelens kod, belopp ur redovisningen (`amount`), chip `Oplanerad` (samma som routes.ts:8006) när `oplanerad=true` med förklaringsraden när någon rad är märkt; obokfört kvitto står med *ej bokförd* i dämpad stil i beloppskolumnen, aldrig som ett tal; inga bundna kostnader ⇒ tom-förklaring (bindningen görs av svepet och kön, aldrig här).
**KRAV-5** | `.pengarad` läggs i `html.ts` enligt 1D:s klasstabell (tal i `--mono`, högerställda, "ej bokförd" i `--muted`) — enda nya klassen; resten bärs av `.panel`/`.chip`/`.table-wrap`/`.subnav`/`.farskhet` och inline-stil som övriga uppdragssidor.
**KRAV-6** | Ingen progressbar, ingen procent, inga skrivvägar: sidan saknar `<progress>`, procenttal i synligt textinnehåll, `<script>`, formulär och knappar; SVG:n bär `role="img"` + `aria-label` och ramdatumen/villkoren står som text utanför bilden — bilden är aldrig enda bäraren.
**KRAV-7** | Rutt `GET .../pengarna` via `pageFor`-mönstret; posten `['pengarna', 'Pengarna']` i `UPPDRAGSSIDOR` (routes.ts:3531) mellan `leveranserna` och `kontraktet` (1E Del 5:s ordning); `aria-current="page"` på exakt en subnav-post.
**KRAV-8** | vitest i `server/test/uppdragsytan-pengarna.test.ts` genom hela stacken (mönster `uppdragsytan-laget.test.ts`): kurva + två datum i klartext vid bekräftat tak, taxa och bokad framtid; ALLA TRE villkorsfallen ('inget bekräftat tak', 'ingen taxa', 'ingen bokad framtid') ⇒ ingen kurva/inget datum och villkoret i klartext; "svepet har inte kört"-fallet; frånvaro av `<progress>`/procenttal/`<script>`/formulär; `Oplanerad`-chip på märkt rad; seriens slutsumma == rotdelens förbrukning (tjänstenivå); användare i annat bolag når inte sidan.
**KRAV-9** | `docs/STATUS.md` får en rad i sessionsloggen; `docs/MCP_ACTIONS.md` rörs inte (ingen ny åtgärd).
**ACCEPTANS** | `npm run build` och `npm test` gröna med faktisk inklistrad utdata; granskaren ser i renderad HTML att varje tal och datum på sidan är LÄST (cachens exakta datum/villkor, redovisningens belopp, husets takberäkning) och inget räknat i vyn, samt att diffen enbart rör `server/src/services/uppdragPengar.ts` (ny), `server/src/http/view/html.ts` (`.pengarad` + kurvhelper), `server/src/http/view/routes.ts`, `server/test/` och `docs/STATUS.md`.
**AVGRANSNING** | Ingen migration, ingen ny/ändrad åtgärd i registry.ts, ingen sensitivity-ändring, inga nya beroenden; `uppdragSvep.ts`, `uppdragKostnad.ts`, `contracts.ts` och `receipts.ts` rörs inte (svepet/prognosen/tröskeln/bindningen ägs av S7.4/S6.2/S6.1); inga andra nya CSS-klasser än `.pengarad`; övriga `UPPDRAGSSIDOR`-poster orörda.
uteslutet: meningen "sista bokade aktivitet den Y"/X-före-Y-regeln ur 1D 4.4 — källan kräver det inte (Davids ja gäller underlagets två cachade ramdatum)
uteslutet: 1D-tabellkolumnerna Aktivitet/Typ/Debiterbar/Verifikat (leverabelkoppling, kostnadstyp och verifikatlänk finns inte i kvittoläsningen) — källan kräver det inte
uteslutet: obundna kvitton (`contract_part_id IS NULL`) i kostnadstabellen — källan kräver det inte
uteslutet: `.harledning`-klassen (hover-härledning per tal) — källan kräver det inte
uteslutet: ny MCP-/REST-läsåtgärd `las_pengarna` (1E Del 4 listar ingen) — källan kräver det inte
uteslutet: tröskelmarkör eller larmnivåer inritade i kurvan — källan kräver det inte
```

## Utfall
Tester: 132 passed (132) · Granskning: GODKANT | Alla nio krav och avgränsningarna är uppfyllda i diffen: serien byggs med exakt `forbrukningPerRad`:s gruppering/taxeordning/heltalsregel så slutsumman per konstruktion är rotdelens tal, pre · Byggforsok: 3

## Modellkedja (Davids krav 17/8)
Krav+granskning: claude-fable-5 · Utveckling: claude-opus-5 · Claude Code pa Davids abonnemang — inga API-tokens
